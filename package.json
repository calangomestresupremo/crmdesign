{
  "name": "crm-design",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc && cp index.html dist/index.html && cp src/styles.css dist/styles.css",
    "check": "tsc --noEmit",
    "dev": "npm run build && python3 -m http.server 4173 -d dist"
  },
  "devDependencies": {}
}
